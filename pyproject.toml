[project]
name = "algo-sketches"
version = "0.1.0"
description = "A growing collection of algorithm solutions, each with its own test."
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["solutions*"]
